import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { 
    CheckedNavButton,
    Small,
    Default,
    Medium,
    Large,
} from './nav-button.stories';
import NavButton from '../components/atoms/nav-button';
import { MemoryRouter } from 'react-router';

<Meta title="Atoms|NavButton" component={NavButton} />

# NavButton

This section describe all variant of NavButton component. You can play with 3 sizes: `small`, `medium` and `large`.

<div style={{ display: 'flex', justifyContent: 'space-between', width: '600px' }}>
    <MemoryRouter>
        <CheckedNavButton onClick={e => console.log('Clicked')} />
        <Small onClick={e => console.log('Clicked')} />
        <Medium onClick={e => console.log('Clicked')} />
        <Large onClick={e => console.log('Clicked')} />
    </MemoryRouter>
</div>

Now let's see in details !

```javascript
<NavButton size="large" onClick={e => alert('Clicked')}>Large NavButton</NavButton>
```

# Props

<Props of={NavButton} />