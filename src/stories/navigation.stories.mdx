import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { NavigationComponent } from './arrow-navigation.stories';
import Navigation from '../components/atoms/button';

<Meta title="Atoms|Navigation" component={Navigation} />

# Navigation

This section describe the Navigation component.

<div style={{ height: '60px',position: 'relative' }}>
      <NavigationComponent
        testid="navigation"
        checkedIndex={navIndex}
        onClick={(index) => setNavIndex(index)}
        elements={[
          { name: 'Recipes', href: 'recipes' },
          { name: 'About', href: 'about' },
          { name: 'Contact', href: 'contact' },
        ]}
      />,
</div>

Now let's see in details !

```javascript
      <NavigationComponent
        testid="navigation"
        checkedIndex={navIndex}
        onClick={(index) => setNavIndex(index)}
        elements={[
          { name: 'Recipes', href: 'recipes' },
          { name: 'About', href: 'about' },
          { name: 'Contact', href: 'contact' },
        ]}
      />,
```

# Props

<Props of={Navigation} />